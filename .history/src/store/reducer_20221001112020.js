import { useReducer } from "react";

const initState = {
  todos: [],
  todoInput: "",
};
function useReducer(state, action) {
  switch (action.type) {
  }
}

export default useReducer;
export { initState };
