import { useReducer } from "react";

const initState = {
  todos: [],
  todoInput: "",
};
function reducer(state, action) {
  switch (action.type) {
  }
}

export default reducer;
export { initState };
